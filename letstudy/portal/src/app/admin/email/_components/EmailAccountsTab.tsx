"use client";

// EmailAccount (SMTP) has been removed — Resend API is now used exclusively.
// This file is kept to avoid import errors but renders nothing.
export function EmailAccountsTab() {
  return null;
}
